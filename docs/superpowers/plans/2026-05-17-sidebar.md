# Sidebar 会话列表实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 添加固定侧边栏，支持选择历史会话和创建新会话。

**Architecture:** 后端新增 `GET /agent/threads` API 返回用户会话列表，前端新增 Sidebar 组件和 useThreadList hook，修改 page.tsx 使用 flex 布局。

**Tech Stack:** NestJS + Prisma (Backend), Next.js + React (Frontend)

---

## File Structure

### Backend
- Create: `server/src/agent/dto/get-threads-query.dto.ts` — Query 参数 DTO
- Create: `server/src/agent/dto/thread-item.dto.ts` — 响应项 DTO
- Modify: `server/src/agent/agent.controller.ts` — 新增 GET /agent/threads 端点
- Modify: `server/src/agent/agent.service.ts` — 新增 getThreads 方法

### Frontend
- Create: `web/src/hooks/use-thread-list.ts` — 会话列表 Hook
- Create: `web/src/components/sidebar.tsx` — 侧边栏组件
- Modify: `web/src/app/page.tsx` — 添加 Sidebar 布局

---

## Task 1: Backend - 创建 DTOs

**Files:**
- Create: `server/src/agent/dto/get-threads-query.dto.ts`
- Create: `server/src/agent/dto/thread-item.dto.ts`

### Step 1: 创建 GetThreadsQueryDto

```typescript
import { IsString, IsNotEmpty } from 'class-validator';

export class GetThreadsQueryDto {
  @IsString()
  @IsNotEmpty()
  userId: string;
}
```

### Step 2: 创建 ThreadItemDto

```typescript
export class ThreadItemDto {
  threadId: string;
  name: string;
  createdAt: string;
  agentId: string;
}
```

### Step 3: Commit

```bash
git add server/src/agent/dto/get-threads-query.dto.ts server/src/agent/dto/thread-item.dto.ts
git commit -m "feat(agent): add DTOs for GET /agent/threads"
```

---

## Task 2: Backend - Controller 新增端点

**Files:**
- Modify: `server/src/agent/agent.controller.ts`

### Step 1: 添加 Get 导入

```typescript
import { Get, Query } from '@nestjs/common';
```

### Step 2: 添加新端点

```typescript
@Get('threads')
async getThreads(@Query() query: GetThreadsQueryDto) {
  this.logger.log(`Getting threads for user: ${query.userId}`);
  return this.agentService.getThreads(query);
}
```

### Step 3: Commit

```bash
git add server/src/agent/agent.controller.ts
git commit -m "feat(agent): add GET /agent/threads endpoint"
```

---

## Task 3: Backend - Service 实现 getThreads

**Files:**
- Modify: `server/src/agent/agent.service.ts`

### Step 1: 添加 DTOs 导入

```typescript
import { GetThreadsQueryDto } from './dto/get-threads-query.dto';
import { ThreadItemDto } from './dto/thread-item.dto';
```

### Step 2: 添加 getThreads 方法

```typescript
async getThreads(query: GetThreadsQueryDto): Promise<{ data: ThreadItemDto[] }> {
  const { userId } = query;

  // 查询用户的所有线程
  const threads = await this.prisma.agentThread.findMany({
    where: { user_id: userId },
    orderBy: { created_at: 'desc' },
    include: {
      chats: {
        orderBy: { created_at: 'asc' },
        take: 1, // 只取第一条 chat
        include: {
          messages: {
            where: { role: 'user' },
            orderBy: { created_at: 'asc' },
            take: 1, // 只取第一条用户消息
          },
        },
      },
    },
  });

  const data: ThreadItemDto[] = threads.map((thread) => {
    // 获取第一条用户消息作为名称
    let name = '新会话';
    const firstChat = thread.chats[0];
    if (firstChat) {
      const firstUserMessage = firstChat.messages[0];
      if (firstUserMessage) {
        const content = JSON.parse(firstUserMessage.content);
        const text = content?.text || '';
        name = text.substring(0, 30) + (text.length > 30 ? '...' : '');
      }
    }

    return {
      threadId: thread.uuid,
      name,
      createdAt: new Date(Number(thread.created_at)).toISOString(),
      agentId: thread.agent_id.toString(),
    };
  });

  return { data };
}
```

### Step 3: Commit

```bash
git add server/src/agent/agent.service.ts
git commit -m "feat(agent): implement getThreads method"
```

---

## Task 4: Frontend - 创建 useThreadList Hook

**Files:**
- Create: `web/src/hooks/use-thread-list.ts`

### Step 1: 创建 Hook

```typescript
'use client';

import { useState, useCallback } from 'react';

export interface ThreadItem {
  threadId: string;
  name: string;
  createdAt: string;
  agentId: string;
}

export interface UseThreadListResult {
  threads: ThreadItem[];
  loadThreads: () => Promise<void>;
  isLoading: boolean;
}

/**
 * useThreadList - 获取用户会话列表
 * @param userId - 用户 ID
 */
export function useThreadList(userId: string): UseThreadListResult {
  const [threads, setThreads] = useState<ThreadItem[]>([]);
  const [isLoading, setIsLoading] = useState(false);

  const API_URL = process.env.NEXT_PUBLIC_AGENT_API_URL || 'http://localhost:3000';

  const loadThreads = useCallback(async () => {
    if (!userId) return;

    setIsLoading(true);
    try {
      const response = await fetch(`${API_URL}/agent/threads?userId=${encodeURIComponent(userId)}`);
      if (!response.ok) throw new Error(`HTTP error: ${response.status}`);
      const result = await response.json();
      setThreads(result.data || []);
    } catch (error) {
      console.error('Failed to load threads:', error);
    } finally {
      setIsLoading(false);
    }
  }, [userId]);

  return {
    threads,
    loadThreads,
    isLoading,
  };
}
```

### Step 2: Commit

```bash
git add web/src/hooks/use-thread-list.ts
git commit -m "feat(web): add useThreadList hook"
```

---

## Task 5: Frontend - 创建 Sidebar 组件

**Files:**
- Create: `web/src/components/sidebar.tsx`

### Step 1: 创建组件

```typescript
'use client';

import { ThreadItem } from '@/hooks/use-thread-list';

interface SidebarProps {
  threads: ThreadItem[];
  currentThreadId: string;
  onSelectThread: (threadId: string) => void;
  onNewThread: () => void;
  isLoading: boolean;
}

export function Sidebar({ threads, currentThreadId, onSelectThread, onNewThread, isLoading }: SidebarProps) {
  return (
    <div style={{
      width: '280px',
      borderRight: '1px solid #e5e5e5',
      background: '#f9f9f9',
      display: 'flex',
      flexDirection: 'column',
      height: '100%',
    }}>
      {/* 新建会话按钮 */}
      <div style={{ padding: '1rem' }}>
        <button
          onClick={onNewThread}
          style={{
            width: '100%',
            padding: '0.75rem 1rem',
            border: '1px solid #e5e5e5',
            borderRadius: '6px',
            background: '#fff',
            color: '#333',
            fontSize: '0.875rem',
            fontWeight: 500,
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            gap: '0.5rem',
          }}
        >
          <span style={{ fontSize: '1.25rem' }}>+</span>
          新建会话
        </button>
      </div>

      {/* 会话列表 */}
      <div style={{ flex: 1, overflow: 'auto' }}>
        {isLoading ? (
          <div style={{ padding: '1rem', textAlign: 'center', color: '#999' }}>
            加载中...
          </div>
        ) : threads.length === 0 ? (
          <div style={{ padding: '1rem', textAlign: 'center', color: '#999' }}>
            暂无会话
          </div>
        ) : (
          threads.map((thread) => (
            <div
              key={thread.threadId}
              onClick={() => onSelectThread(thread.threadId)}
              style={{
                padding: '0.75rem 1rem',
                cursor: 'pointer',
                background: thread.threadId === currentThreadId ? '#e5e5e5' : 'transparent',
                borderLeft: thread.threadId === currentThreadId ? '3px solid #007AFF' : '3px solid transparent',
              }}
            >
              <div style={{
                fontSize: '0.875rem',
                color: '#333',
                overflow: 'hidden',
                textOverflow: 'ellipsis',
                whiteSpace: 'nowrap',
              }}>
                {thread.name}
              </div>
              <div style={{
                fontSize: '0.75rem',
                color: '#999',
                marginTop: '0.25rem',
              }}>
                {new Date(thread.createdAt).toLocaleString('zh-CN', {
                  month: 'numeric',
                  day: 'numeric',
                  hour: '2-digit',
                  minute: '2-digit',
                })}
              </div>
            </div>
          ))
        )}
      </div>
    </div>
  );
}
```

### Step 2: Commit

```bash
git add web/src/components/sidebar.tsx
git commit -m "feat(web): add Sidebar component"
```

---

## Task 6: Frontend - 修改 Page 布局

**Files:**
- Modify: `web/src/app/page.tsx`

### Step 1: 添加 Sidebar 导入

```typescript
import { AgentChat } from '@/components/agent-chat';
import { Sidebar } from '@/components/sidebar';
import { useThreadList } from '@/hooks/use-thread-list';
```

### Step 2: 修改布局 - 使用 flex 水平布局

将原来的：
```tsx
<div style={{ height: '100vh', display: 'flex', flexDirection: 'column' }}>
```

改为：
```tsx
<div style={{ height: '100vh', display: 'flex', flexDirection: 'row' }}>
```

### Step 3: 在 header 前添加 Sidebar

在 `<header ...>` 前添加：

```tsx
{isConfigValid && (
  <Sidebar
    threads={threads}
    currentThreadId={threadId}
    onSelectThread={(newThreadId) => {
      setThreadId(newThreadId);
    }}
    onNewThread={handleNewThread}
    isLoading={isLoadingThreads}
  />
)}
```

### Step 4: 添加 state 和 hook

在组件内添加：

```typescript
const [threads, setThreads] = useState<ThreadItem[]>([]);
const { loadThreads, isLoading: isLoadingThreads } = useThreadList(userId);
const [threadsLoaded, setThreadsLoaded] = useState(false);
```

### Step 5: 添加 useEffect 加载会话列表

```typescript
// 加载会话列表
useEffect(() => {
  if (userId) {
    loadThreads().then(() => setThreadsLoaded(true));
  }
}, [userId, loadThreads]);
```

### Step 6: 添加 handleNewThread

```typescript
const handleNewThread = () => {
  if (!selectedAgent) return;
  const newThreadId = crypto.randomUUID();
  localStorage.setItem(`thread_${selectedAgent}`, newThreadId);
  setThreadId(newThreadId);
};
```

### Step 7: 导入 ThreadItem 类型

```typescript
import { ThreadItem } from '@/hooks/use-thread-list';
```

### Step 8: Commit

```bash
git add web/src/app/page.tsx
git commit -m "feat(web): add sidebar layout to page"
```

---

## Spec Coverage Check

| Spec Requirement | Task |
|------------------|------|
| GET /agent/threads API | Task 1, 2, 3 |
| DTOs: GetThreadsQueryDto, ThreadItemDto | Task 1 |
| getThreads method | Task 3 |
| useThreadList hook | Task 4 |
| Sidebar component | Task 5 |
| Page layout with sidebar | Task 6 |

---

**Plan complete.**
